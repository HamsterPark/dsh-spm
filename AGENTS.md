# AGENTS.md — 仓库导览与工作约定

本文件面向在本仓库中审阅或修改代码的智能体。先按任务选择阅读路径；迁移批次、历史测量与维护细则按需读取。

## 项目定位

`dsh-spm` 将 Python/LangGraph 系统 MAST 的 STM/SPM（扫描隧道显微镜／扫描探针显微镜）领域能力迁移为 TypeScript 的 DeepSeek Harness（dsh）插件。仪器协议、状态管理、安全检查、科学计算与技能实现由本仓维护；会话、模型编排、审批与应用外壳交给 dsh。

MAST 是只读的参考实现，其真实仪器使用经历属于项目背景。本插件的分发安装、模型调用、端到端执行与硬件验证有各自的验收边界，不能由参考系统的经历或单元测试结果代替。

## 从哪里开始

| 任务 | 先读 | 用途 |
|---|---|---|
| 了解或评审项目 | [README](README.md) → [Review guide](docs/REVIEW-GUIDE.md) | 项目范围、代表性代码、可复核证据与已知限制 |
| 修改代码、迁移技能 | 本文件 → [开发与验证](docs/DEVELOPMENT.md) → 改动附近的实现与测试 | 开发约束、生成顺序、验收和并行协作 |
| 判断能否运行 | [最小运行清单](docs/MINIMUM-RUN-TODO.md) | 开发态装载、分发安装、模型调用和模拟器链路分别到哪一步 |
| 继续技能迁移 | [迁移清单](docs/MIGRATION-TODO.md) + [生成的进度](spec/progress.json) | 未迁移能力、依赖阻塞和明确排除项 |
| 升级 dsh | [版本事实](docs/dsh/facts.md) + [升级策略](docs/dsh/upgrades.md) | 已锁版本、上游接触面与通道策略 |
| 准备公开发布 | [发布清单](docs/RELEASE-TODO.md) | 公开内容、来源、历史与发布阻塞 |

只读审阅不需要私有 MAST、仪器、模型密钥或模拟器。按用户要求决定是否执行验证；不要因为读取本文件就启动迁移、升级依赖、重导金样或连接设备。

## 怎样解释证据

- **迁移数量**以 [spec/progress.json](spec/progress.json) 为准；计数逻辑在 [build-progress.ts](scripts/build-progress.ts)。其中 `done` 表示实现已注册、生成规格存在、参考模型 schema 存在、至少有一条参考轨迹。它不逐项证明集成测试、变异演练或运行验收已通过。
- **行为与兼容性**要同时看实现、测试、对应金样和 [偏差登记](spec/deviations.md)。测试通过说明其输入与断言范围内成立；生成声明与源声明相同不构成独立行为验证。
- **测试和 CI 结果**必须绑定提交或工作树、环境、命令及范围。交接日志是历史记录；引用时注明日期与范围，不把某批演练或某平台的结果扩展为当前全仓结论。
- **无法验证的部分**写清缺少什么条件。静态阅读、实际执行与历史记录分别陈述；文档和代码不一致时指出差异。运行边界见最小运行清单，已记录的 CI 失败见发布清单。

## 代码地图

| 路径 | 职责 |
|---|---|
| `packages/host/kernel/` | 技能执行内核、安全闸、单位解析、参数与前置条件 |
| `packages/host/numerics/`、`vision/` | 数值计算与图像判据；差分测试按算法声明精度或容差 |
| `packages/host/stm-skills/` | 模型工具适配、单技能与组合流程 |
| `packages/host/stm-safety/`、`stm-records/`、`nanonis-files/` | 安全服务、记录与仪器文件格式 |
| `packages/host/compat/` | 集中适配 dsh API，并以契约测试检查上游行为 |
| `packages/instrument/` | TCP 协议、连接、状态缓存、看门狗与模拟器进程 |
| `packages/client/`、`packages/bundle/` | 客户端接入与插件打包入口；运行状态见最小运行清单 |
| `spec/`、`tools/spec-export/`、`tools/mutate/` | 参考输出、偏差与进度，以及导出和变异验证工具 |

## 修改时必须遵守

1. **保留现有工作。** 开始先看 `git status --short` 和相关 diff，明确本次修改范围与验收命令。不覆盖、还原或提交别人的未完成修改。
2. **参考仓只读。** 执行导出前读对应脚本，确认参考源码路径与隔离运行目录，检查前后写入；`MAST_ROOT`（参考源码）与 `MAST2_PROJECT_ROOT`（运行目录）不得混用。具体流程见开发指南。私有参考源码、研究资料、凭据与原始仪器数据不得进入本仓历史。
3. **Phase 8 之前不连接真实仪器，不创建 `profiles/mast-rig`。** 当前验证使用测试替身或明确配置的模拟器。目录缺失只是配置约束，不能据此推断任意执行路径都无法访问硬件。
4. **上游导入集中于 compat。** 除 `compat/src` 自身外，生产代码经 `dsh-spm-compat` 使用 `@deepseek-ai/*`；`contract/` 测试可直接引用真实上游包。类型声明增补与元数据字符串不是运行时导入。实际检查见 [boundary.test.ts](packages/host/compat/src/boundary.test.ts)。
5. **保持可观察契约。** 参数、单位、返回字段、拒绝文案与错误分支都是接口。参考实现的缺陷不必照抄，但行为差异须登记原因及改变决定所需的证据。
6. **生成物由来源更新。** 不为通过测试而改金样或手填进度。改规格后按 `gen:skills` → `build` → `gen:progress` 执行；重导私有参考内容后检查公开文本，保留真实来源类别。生成物及脱敏例外见开发指南。
7. **验证失败不得隐藏。** 不用跳过失败用例、削弱断言或降低覆盖率门槛使结果变绿。先分清产品缺陷、环境差异、过期生成物与测试问题，再修对应来源并记录验证范围。
8. **变异演练独占工作树。** 它会临时改源码并重建，不能与同一工作树中的编辑或其他演练并行。完整入口和中断处理见开发指南；普通审阅无需运行它。

## 常用验证入口

运行环境以 [package.json](package.json) 的 `engines` 和 `packageManager` 为准；CI 配置覆盖 Node 22.19 / 24 与 Windows / Ubuntu。以下从仓库根目录执行，PowerShell 与 POSIX shell 均可用：

```text
pnpm install --frozen-lockfile
pnpm build
pnpm test --project unit --project contract
```

已有依赖时无需重复安装。构建写入 `lib/` 等本地构建产物；上述测试不需要私有参考仓、模型密钥或 STM 模拟器。已知跨平台失败的历史记录在 [发布清单](docs/RELEASE-TODO.md)，本次结果以实际运行输出为准。

`pnpm test` 不带项目筛选会包含集成测试，需要显式设置 `STMSIM_PYTHON` / `STMSIM_ROOT`，并启动占用本地端口的模拟器。缺少前置条件时报告未执行，不把它算作通过。

修改后的检查按影响范围选择：

- 文档：核对路径、链接、命令与证据口径；不需要因此重导金样或运行全套演练。
- 代码：构建、相关单元／契约测试；仪器交互变更还需对应模拟器测试。
- 技能迁移：执行开发指南中的完整完成定义，分别报告已验证项和缺项。

交付时说明改了什么、为什么、跑了哪些检查、结果和剩余限制。报告问题时提供文件位置与触发条件；避免仅凭测试数量或历史叙述判断运行成熟度。
