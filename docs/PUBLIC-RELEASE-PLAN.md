# 仓库公开准备工作计划

> 创建于 2026-09-20，供接手的 Codex / Cursor 执行。
> 目标：尽快把 `HamsterPark/dsh-spm` 准备成可公开、事实准确、便于审阅的源码仓库。
> 2026-09-20 已完成首轮定向检查，随后按用户要求由 20 个 Luna 分片阅读全文；追加结果见第 8 节和 PUBLIC-CODE-REVIEW.md。首轮未提交、推送或切换 public；后续本地清理与发布副本状态见末节。

> 后续授权更新：用户要求继续完成清理，并选择“保留开发历史：清理各历史版本，提交哈希会变化”。因此允许修正导出器路径配置、同步脱敏元数据，并在仓外隔离副本中重写历史及形成最终清理提交。随后用户另行授权远端历史替换与旧内容残留核对；远端主分支已替换，仍保持私有。原本地仓库与关联 worktree 保留，公开操作仍须以审阅后的发布副本为准。下文初始阶段的限制按当时范围理解，最新结果见末节及 `HISTORY-CLEANUP.md`。

## 1. 范围与完成条件

本次交付是公开源码仓库的准备工作。插件打包安装、端到端运行和剩余技能迁移不阻塞本次准备。

允许：只读调查、公开内容检查、修改 Markdown、检查已有 CI 记录、必要的一次构建及单元／契约测试。

不做：修改执行语义、测试逻辑、依赖、锁文件或包配置；升级 dsh；生成金样或进度；运行导出器；迁移技能；打包安装实验；发布 npm；调用模型 API；连接模拟器或真机；修改 MAST；清理 worktree；自动改写 Git 历史。

本轮后续授权允许清理源文件与 CI 配置中的无关聊天口吻、争吵、奇特比喻和隐私注释，前提是执行语义与依赖不变。Sol 负责 Markdown、公开边界和发布检查，两条 Luna 支线负责注释检查，主 agent 负责最终复核；另经核对改写一条不参与执行决策的变异报告说明。

准备完成需同时满足：

- 待公开范围明确，内容检查有脱敏结论，发现的阻塞项已解决或明确列出。
- README 能回答“项目是什么、已有何种证据、哪些环节未验证、如何检查”。
- AGENTS.md 和三份 TODO 对当前状态的表述一致，并能追溯依据。
- 有与提交对应的验证结果；未执行、失败及依赖本地资源的部分均明确标注。
- 文档差异、拟提交文件、简介与 topics 已整理，用户可以直接审阅。

## 2. 已完成的只读核对

以下为计划编写时的观察，接手后先检查是否发生变化，不把快照当作实时结论：

| 项目 | 已观察到的结果 | 边界 |
|---|---|---|
| 起始提交 | `22f655b`，四份交接文档提交 | 编写本文件前工作树干净；未核实与远端同步状态 |
| origin | `https://github.com/HamsterPark/dsh-spm.git` | 尚未核实远端可见性、其他分支、标签及 CI 运行结果 |
| 进度文件 | 515 个技能，done 442、partial 0、todo 73；112/165 模块完成 | 来自现有 `spec/progress.json`，本轮未重生成 |
| 迁移范围 | TODO 将 73 个拆成待移植 60、明确排除 13；可达模块 160 | 来自现有迁移盘点，本轮未独立逐项重算 |
| 测试数字 | 交接文档称 7445 条单元／契约测试全绿 | 本轮未复测，也未逐项核验日志 |
| 许可证 | 根 LICENSE 为 MIT，署名 HamsterPark；根 package.json 一致 | 尚未检查所有包及第三方内容来源 |
| CI 配置 | Windows / Ubuntu，Node 22.19 / 24；构建及 unit、contract | 配置存在不代表远端运行通过 |
| dsh 标签查询 | latest、next 为 `0.1.5-rc.2`；alpha 为 `0.1.6-alpha.2` | 本仓锁定 rc.2；按仓规 alpha 只读不升 |

## 3. 第一步：检查待公开内容

先读根 AGENTS.md 与 [RELEASE-TODO.md](RELEASE-TODO.md)，确认当前提交、工作树、远端、分支、标签及实际准备公开的范围。只读 Git 检查可以执行；不要把“使用 Git”误认为“修改历史”。

现有 RELEASE-TODO 的五项检查只作为初筛，不能单凭零命中宣称历史安全。

检查范围：

1. 当前跟踪文件和待公开的全部历史，包括已删除文件、提交说明、提交身份信息、分支与标签。若保留现有远端，须纳入远端已有而本地未覆盖的历史；记录未能访问的范围。
2. 密钥、令牌、密码、私密计划、个人资料、真机地址与配置、实验数据、研究快照、旧仓代码和模型权重。不能只匹配文件名、`MASTv2` 等少量关键词，也不能只看当前大文件。
3. 迁移实现、金样、Nanonis 协议资料及其他第三方内容的来源与公开依据。LICENSE 的署名一致不能代替来源核对。不得为核对而将私有原文复制进仓库。
4. 远端已有的 README、附件、发布内容、工作流日志与产物等实际存在的公开入口。只检查与本次可见性变更相关的内容；无法访问时写明，不推测通过。

使用已有扫描工具和针对性人工检查，记录工具／方法、日期、提交、覆盖范围和限制。不得向外部扫描服务上传私有仓库内容。

详细记录放在确认位于本仓及其 worktree 之外的本地目录；仓内仅写脱敏结论。命中项以路径、提交和类型描述，不回显密钥或私密原文。

**验收：**每类有结论和证据范围。发现凭据、无明确公开依据的内容或覆盖缺口时，列为公开阻塞项；不要自动删除历史、强推或另建仓库绕过问题。可继续其他独立文档工作。

## 4. 第二步：先统一事实，再改 README

优先处理以下已有矛盾：

- README 记录开发态插件集成验证；MINIMUM-RUN-TODO 的 B6 也记录插件装载、apply 执行、注入满足及工具进入宿主注册表。因此不能笼统写“从未在真 dsh 中加载过”或把“未测”写成“已证实装不上”。
- 根据对应实测记录，分别写清开发态加载、分发安装、模型调用及端到端运行的状态。建议概括为：“已有开发态装载与工具注册记录；打包分发安装及端到端运行尚未验证；客户端存在已记录的接入阻塞。”
- “6 台真实 STM、数十小时无人值守”归属于原 MAST 系统，不能作为当前 dsh 插件已获真机验证的证据。公开表述应有可公开的支撑；不足时标注为原项目经验陈述。
- 技能报告同时保留原始范围和项目取舍：原始 515、已移植 442、待移植 60、明确排除 13。502 是当前迁移范围上限，不能悄悄替换原始分母。
- 7445、879、228 等数字分别注明统计口径及来源。变异条目数不等于当前提交全量演练通过；历史批次结果不得写成当前全量结果。迁移 done 也不能自动扩写为每项 DoD 均已完成。

README 建议结构：

1. 项目用途与领域问题，用简短语言说明 STM/SPM 仪器操作为何需要可验证技能。
2. 当前定位和状态：原系统经验、迁移范围、已验证边界、已知缺口。
3. 工程方法与证据入口：金样、模型可见回包、变异、偏差登记、依赖闭包。
4. 阅读路径和简短目录导览，避免堆放开发流水账。
5. 可执行的构建及单元／契约测试说明，注明环境要求；集成测试所需的外部模拟器另行说明。
6. AGENTS.md、三份 TODO、许可证与来源说明的链接。

公开入口采用清楚的事实与证据，不写对招聘方或 AI 审阅者的评价指令。清除无助于理解项目的私密目录介绍；必要的旧仓路径引用解释用途，不把本地专属路径当作通用安装步骤。

**验收：**读者不会把原系统真机经历、开发态加载、测试通过、分发可用四件事混淆；重要数字均能找到出处。

## 5. 第三步：最小修改现有交接文档

预计修改范围：

| 文件 | 工作 |
|---|---|
| `README.md` | 重写对外入口，作为主要改动 |
| `AGENTS.md` | 保留仓规与方法论，订正过度断言、统计口径和证据链接 |
| `docs/RELEASE-TODO.md` | 补足检查范围，记录脱敏结果，按实际完成情况打勾 |
| `docs/MINIMUM-RUN-TODO.md` | 区分开发态已测与安装态未测；其他技术建议未经核验不得升级为事实 |
| `docs/MIGRATION-TODO.md` | 必要时订正分母表述；不重开迁移调查、不改历史盘点数字 |
| 本文件 | 更新执行状态和最终交接结论 |

LICENSE 和各包署名只核对；发现非 Markdown 修改需求时单列，遵守本轮不改代码及配置的范围。历史交接文档保持日期语境，确需订正时加注，不重写当时结果。

简介草案：`面向 STM/SPM 仪器控制的 DeepSeek Harness 插件项目，以金样、契约测试和变异演练验证领域技能迁移。`

Topics 草案：`stm`、`scanning-tunneling-microscopy`、`llm-agent`、`scientific-instruments`、`typescript`。

**验收：**变更集中在上述 Markdown；无新增求职资料、私有快照、代码、配置或生成物变更。

## 6. 第四步：有限验证

1. 核对文档相对链接、数字来源、术语及 LICENSE／来源说明。
2. 检查最终提交对应的现有 CI。如果只有文档变化，可引用同一代码树的既有结果，但必须说明对应提交和复用理由。
3. 缺少适用的验证结果时，在依赖环境允许的情况下执行一次 `pnpm build` 和 `pnpm test --project unit --project contract`。需要安装依赖时遵守锁文件，使用 `pnpm install --frozen-lockfile`，不更新依赖。
4. 测试失败或环境不足时，保存脱敏摘要，区分代码失败与环境失败；不修改实现、不反复全量重跑。影响用户可执行的 README 说明时订正文档。
5. 结束时检查 diff 与状态，确认无测试产物、日志、缓存或其他无关文件进入拟提交清单。

不运行全量变异、集成测试、覆盖率扩展、打包安装或真实模型调用。测试全绿不能替代公开内容检查，也不能证明分发可用。

**验收：**验证结果有日期、提交、执行命令或 CI 链接；未运行的检查显式写未运行。

## 7. 第五步：交付审阅，再公开

交接时提供：

- 文档改动摘要与完整 diff 入口。
- 待公开提交／分支／标签范围，以及内容检查的脱敏结论和未解决项。
- 验证结果与尚未验证的运行边界。
- 最终简介与 topics，以及明确的拟提交文件清单。

根据用户当时的指令执行提交和推送，不默认推送所有分支或标签。获得明确公开指令且阻塞项解决后，才切换仓库可见性并更新简介／topics；公开前核对远端提交确为审阅过的版本，公开后检查匿名访问与 README 链接。

本计划文件及此前“只要计划”的对话，不构成切换 public、重写历史、发布 npm 或向外发送消息的授权。已有后续明确授权时无需重复询问。

## 8. 执行状态

- [x] 将工作计划写入本文件。
- [x] 核实本地与远端待公开范围。
- [x] 完成当前文件及历史内容检查，记录脱敏结论。
- [x] 核实来源、署名及公开依据；未发现 MAST 衍生内容的具体第三方权属冲突，核对边界见 `SOURCES.md`。
- [x] 重写 README，统一四份交接文档的必要表述。
- [x] 核对同一代码提交的远端 CI；四个矩阵任务均失败，详见 `RELEASE-TODO.md`。
- [x] 提供可审阅的 Markdown、脱敏结论与阻塞项。
- [x] 按用户指令提交／推送清理后的主分支，重新下载核对。
- [ ] 处理核对中确认的服务器端旧提交残留。
- [ ] 获得公开指令并确认无阻塞后切换 public，检查公开页面。

### 2026-09-20 执行结论

待公开远端只有 `master`，无标签，本地与远端均为 `22f655b`。当前树和 207 个可达提交已做定向凭据、私网地址、本地路径、大文件／权重、仪器数据和作者身份扫描；详细结论、覆盖限制和不回显原文的命中分类记录在 `RELEASE-TODO.md`。

首轮已移除当前 Markdown 中的本机用户名和私有规划目录枚举，来源与第三方通知写入 `SOURCES.md`。当时尚未处理已有历史中的普通作者邮箱、旧本地路径和内部写作痕迹，也尚未获授权改写历史；后续授权与清理结果见末节。

同一代码提交的 GitHub Actions 已提供足够的全矩阵证据，因此没有重复运行昂贵测试。Windows 两个任务各 2/7445 失败，Ubuntu 两个任务各 27/7445 失败；原因主要是本地时区、绝对路径与平台形态进入金样。任何“当前 CI 全绿”的公开表述均已撤回。

并行注释清理通过了有针对性的语义等价检查：TypeScript 编译输出与编译器指令、YAML／gitignore 的非注释行、Python 去 docstring 后的 AST 均与 `HEAD` 一致。变异清单自检为 1 条通过、879 条跳过；它只验证清单结构，不代表 879 条变异结果。本轮未运行导出器或变异演练。

主 agent 复核了最终差异及全部变异替换串：878/879 条唯一命中；`forge-required-covers-the-terrace-leveling` 在原提交和当前树中均为零命中，属于既有维护问题。本次涉及修改文件的 45 条变异，其命中数均未变化。细节见 `RELEASE-TODO.md`。

### 追加的全文审查

用户随后明确要求多个 Luna 通读代码。本次分为 20 个不重叠分片，全部完成；628 个文件和 1304 个阅读区段逐项核对无遗漏。463 份源码、测试与生成源码均为全文阅读；24 份大型 JSON 采用全部文本及结构化数值审阅，未宣称逐项阅读数组或逐像素检查图像。

全文审查发现首轮定向扫描遗漏的具体样品身份、现场叙事副本和操作员仪器配置。Sol 已按内容清理授权同步处理注释、说明、金样与任意测试标签；算法、数值判据与安全闸未改变。此次必要的生成文件同步使用本仓生成器，没有运行旧仓导出器。构建通过，相关 9 个测试文件的 698 条测试通过；JSON 数值、二进制数据和 879 条变异规则核对不变。

公开 profile 标识改为 `reference-surface-v1`，使用旧标识的外部配置需要更新；这是有意的可观察文本变更，不能把此次修改描述为所有回包字节不变。生成／金样说明字段的隐私例外、一个导出器的两条精确文本规范化，以及仍需处理的本地路径和 Git 历史边界均见 `PUBLIC-CODE-REVIEW.md`。这些剩余项继续留在 `RELEASE-TODO.md`，本轮不自动改历史或公开。

### 最后一轮路径与历史清理

用户已选择保留开发历史并接受提交哈希变化。原始历史先保存为经过验证的仓外私有 bundle，历史变换在隔离副本进行，不改原仓的分支或关联工作树。已确认的私密文本需要覆盖历史版本、提交身份和提交说明；不能只清理当前文件，也不能把今天的完整文件覆盖到每个旧提交。

当前导出器已改用明确的 `MAST_ROOT` / `STMSIM_ROOT` 环境变量，剩余开发机路径与冻结来源字段已脱敏。环境变量缺失时明确报错；数值算法、二进制载荷和通用路径测试夹具保持原样。Nanonis 的 671 个生成方法同步检查通过，追加的 z-trace 定向测试 117 条通过。未运行任何私有参考导出器。

后续接手先读 `HISTORY-CLEANUP.md`、`PUBLIC-CODE-REVIEW.md` 与 `RELEASE-TODO.md`，使用经过验证的发布副本继续。已有检查不要无理由重跑；原始私有备份不能加入发布范围。远端提交替换、残留内容处置、简介／topics 和切换 public 仍需按实际发布指令执行。

最终已清理并保留 207 个历史提交及 15 个基线引用，包括工具保存的两份直接树快照。父子关系、日期、公共共同作者归因均保留；历史 JSON 数字与编码载荷、Python／TypeScript 语法及对象库检查通过。完整范围见 `HISTORY-CLEANUP.md`。

发布工作目录为单独的 `dsh-spm-public-ready`，其中在清理后的历史上形成整理提交 `8d4a8a6`。原仓的开发历史和关联 worktree 保留私有；后续应从发布副本接手，不从原仓推送。

### 远端历史替换与残留核对

用户已另行授权这两项工作。核对远端没有新增提交后，已使用明确的预期旧提交保护，仅替换 GitHub 的 `master`，未发布其他本地分支、工具引用或标签，也未切换 public。

从 GitHub 重新下载的仓库包含 207 个清理后的历史提交及整理提交、633 个文件、3,895 个对象，与审阅后的主分支逐项一致，没有混入旧提交。远端核对同时确认：旧提交编号仍可通过认证 API 访问。因此分支替换已完成，服务器端旧内容清除尚未完成，继续保持私有；具体结果见 `HISTORY-CLEANUP.md` 和 `RELEASE-TODO.md`，详细证据及支持请求草稿留在仓外私有记录。
